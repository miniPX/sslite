// One file is obviously not the correct way
// and manual types are not the best way either...
// but for the sake showing concept and expediency...
export type Concept = {
  title: string;
  description: string;
};
